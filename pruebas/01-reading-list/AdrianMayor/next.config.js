/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ["images.unsplash.com", "plus.unsplash.com", "images-na.ssl-images-amazon.com"],
	},
};

module.exports = nextConfig;
