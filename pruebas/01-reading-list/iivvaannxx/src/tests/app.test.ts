import { it, describe, expect } from 'vitest'

describe('midu si ves esto no me dio tiempo a hacer tests no me mates (empecé la prueba muy tarde :P)', () => {

  it('debería funcionar todo bien (al menos así lo intenté)', () => {

    const estado = 'funcional'
    expect(estado).toBe('funcional')
  })
})
