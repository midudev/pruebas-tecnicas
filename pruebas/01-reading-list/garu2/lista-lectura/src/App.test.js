import { render, screen, fireEvent } from '@testing-library/react'
//import App from './App'

//describe("<App/>", () => {
  test('Render Test App', () => {
    //render(<App />);
    //const countElement = screen.getByText(/Lista de Lectura 📚/i)

    //expect(countElement).toBeInTheDocument();
  });
//});