export const FILTER_TYPES = Object.freeze({
  pages: 'pages',
  genres: 'genres',
})

export const DEFAULT_FILTERS = Object.freeze({
  pages: -1,
  genres: -1,
})

export const DEFAULT_HOW_MANY_BOOKS_ARE = Object.freeze({
  numberOfAvailableBooks: 0,
  numberOfBooksInReadingList: 0,
})

export const DEFAULT_MAX_AND_MIN_PAGES = Object.freeze({
  maxPage: 0,
  minPage: 0,
})
