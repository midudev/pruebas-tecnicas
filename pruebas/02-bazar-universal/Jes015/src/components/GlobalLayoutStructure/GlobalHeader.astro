---
import { getFrontRoutes } from "@/config"

---

<header class="border border-primary p-3 bg-secondary flex items-center gap-5">
    <a href={getFrontRoutes().home} class="header__title drop-shadow-text font-bold">Midu Bazar</a>
</header>

<style>
    .header__title {
		font-size: clamp(1.2em, 4vw, 2em)
	}
</style>